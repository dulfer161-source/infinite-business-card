# 🔄 Инструкция по очистке кэша

## Проблема: "Failed to fetch dynamically imported module"

Эта ошибка возникает когда браузер использует старую закэшированную версию сайта.

---

## ✅ Решение 1: Hard Refresh (Быстрый способ)

### Windows / Linux:
```
Ctrl + Shift + R
или
Ctrl + F5
```

### macOS:
```
Cmd + Shift + R
или
Cmd + Option + R
```

---

## ✅ Решение 2: Очистка кэша браузера

### Google Chrome / Edge:
1. Открыть DevTools: `F12` или `Ctrl + Shift + I`
2. Правой кнопкой на кнопку Обновить (↻) в адресной строке
3. Выбрать **"Очистить кэш и жёсткое обновление"**

### Firefox:
1. Открыть DevTools: `F12`
2. Перейти в Network (Сеть)
3. Правой кнопкой → **"Clear Browser Cache"**
4. Обновить страницу: `Ctrl + Shift + R`

### Safari:
1. `Cmd + Option + E` — очистить кэш
2. `Cmd + R` — обновить страницу

---

## ✅ Решение 3: Удаление Service Worker вручную

### Все браузеры:
1. Открыть DevTools: `F12`
2. Перейти в **Application** (Chrome/Edge) или **Storage** (Firefox)
3. В левой панели найти **Service Workers**
4. Нажать **Unregister** напротив активного Service Worker
5. В разделе **Cache Storage** удалить все кэши (infinity7-*)
6. Закрыть и открыть сайт заново

---

## ✅ Решение 4: Режим инкогнито

Откройте сайт в режиме инкогнито/приватном режиме:

### Chrome / Edge:
```
Ctrl + Shift + N
```

### Firefox:
```
Ctrl + Shift + P
```

### Safari:
```
Cmd + Shift + N
```

---

## 🔧 Для разработчиков

### Проверка версии Service Worker:

```javascript
// В консоли браузера (F12)
navigator.serviceWorker.getRegistration().then(reg => {
  console.log('Active SW:', reg.active);
  console.log('Installing SW:', reg.installing);
  console.log('Waiting SW:', reg.waiting);
});
```

### Принудительное обновление:

```javascript
// В консоли браузера (F12)
navigator.serviceWorker.getRegistration().then(reg => {
  reg.update();
  setTimeout(() => window.location.reload(), 1000);
});
```

### Полная очистка:

```javascript
// В консоли браузера (F12)
// 1. Удаляем Service Worker
navigator.serviceWorker.getRegistration().then(reg => {
  if (reg) reg.unregister();
});

// 2. Очищаем все кэши
caches.keys().then(keys => {
  keys.forEach(key => caches.delete(key));
});

// 3. Перезагружаем
setTimeout(() => window.location.reload(), 1000);
```

---

## 📋 Что изменилось

В последнем обновлении:
- ✅ Обновлена версия Service Worker: `infinity7-v2-2025-12-17`
- ✅ Добавлена автоматическая очистка старых кэшей
- ✅ Добавлена проверка обновлений каждую минуту
- ✅ Автоматическая перезагрузка при обновлении

Service Worker теперь сам удалит старые кэши при следующей загрузке.

---

## ⚠️ Если ничего не помогло

1. Закройте ВСЕ вкладки с сайтом
2. Закройте браузер полностью
3. Откройте браузер
4. Откройте сайт заново
5. Если всё ещё ошибка → используйте другой браузер

---

## 🚀 Профилактика в будущем

Для избежания проблем с кэшем:

1. **Периодически обновляйте браузер** — старые версии хуже работают с Service Workers
2. **Используйте Hard Refresh** при обновлениях сайта
3. **Не блокируйте JavaScript** — Service Worker не работает без JS
4. **Разрешите кэширование** — не используйте режим "без кэша" постоянно

---

## 📞 Техподдержка

Если проблема сохраняется:
- Telegram: https://t.me/+QgiLIa1gFRY4Y2Iy
- Email: support@visitka.site

При обращении укажите:
- Браузер и версия (Chrome 120, Firefox 121, etc.)
- Операционная система (Windows 11, macOS 14, etc.)
- Скриншот ошибки из консоли (F12 → Console)

---

**Последнее обновление:** 2025-12-17  
**Версия Service Worker:** infinity7-v2-2025-12-17
