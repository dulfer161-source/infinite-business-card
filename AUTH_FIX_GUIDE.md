# 🔧 Руководство по исправлению авторизации

## ✅ Выполненные исправления

### 1. Telegram Widget (src/components/auth/SocialAuthButtons.tsx)
**Проблема:** Глобальный callback `onTelegramAuthCallback` определялся после загрузки скрипта виджета.

**Исправление:**
```typescript
useEffect(() => {
  // Создаём глобальный callback ПЕРЕД загрузкой скрипта
  (window as any).onTelegramAuthCallback = (user: any) => {
    console.log('Telegram auth callback:', user);
    onTelegramAuth(user);
  };

  const container = document.getElementById('telegram-login-container');
  if (container && !container.hasChildNodes()) {
    const script = document.createElement('script');
    // ... загрузка скрипта
  }

  return () => {
    // Очищаем глобальный callback при размонтировании
    delete (window as any).onTelegramAuthCallback;
  };
}, [onTelegramAuth]);
```

### 2. VK OAuth
**Статус:** Код работает корректно, но требует настройки секретов.

**Что работает:**
- ✅ GET endpoint получает auth_url
- ✅ Редирект на VK OAuth
- ✅ Callback обработка и создание пользователя
- ✅ Автоматическое создание визитки для нового пользователя

## 🔑 Требуемые секреты

### 1. VK OAuth (критично для VK авторизации)
Получить на https://dev.vk.com/

```bash
VK_APP_ID=ваш_app_id           # ID приложения VK
VK_SECRET_KEY=ваш_secret_key   # Защищённый ключ приложения
```

**Настройка VK приложения:**
1. Создать приложение на https://dev.vk.com/apps
2. Тип: "Веб-сайт"
3. Доверенный redirect URI: `https://preview--infinite-business-card.poehali.dev/auth/vk`
4. Включить Open API: да

### 2. JWT Secret (критично для токенов)
```bash
JWT_SECRET=ваш_секретный_ключ_32_символа
```

Сгенерировать: `openssl rand -hex 32`

### 3. Telegram Bot (опционально)
```bash
TELEGRAM_BOT_TOKEN=ваш_telegram_bot_token
```

Получить через @BotFather:
1. `/newbot` - создать бота
2. `/setdomain` - добавить домен visitka.site
3. Получить токен

### 4. YandexGPT (для AI-генерации)
```bash
YANDEX_API_KEY=ваш_yandex_api_key
YANDEX_FOLDER_ID=ваш_folder_id
```

Получить на https://console.yandex.cloud:
1. Сервисные аккаунты → Создать
2. Назначить роль `ai.languageModels.user`
3. Создать API-ключ

## 🐛 Известные проблемы и решения

### Проблема 1: "VK credentials not configured"
**Причина:** Секреты VK_APP_ID или VK_SECRET_KEY не настроены

**Решение:**
1. Добавить секреты в Yandex Cloud Secrets Manager
2. Привязать секреты к функции vk-auth
3. Пересобрать функцию: `sync_backend()`

### Проблема 2: Telegram виджет не загружается
**Причина:** Telegram bot не настроен или неверный домен

**Решение:**
1. Проверить имя бота в `SocialAuthButtons.tsx` (сейчас: `Visitka_sitebot`)
2. Добавить домен в настройках бота через @BotFather: `/setdomain`

### Проблема 3: "Failed to fetch" при VK callback
**Причина:** CORS или неверный redirect_uri

**Решение:**
1. Проверить trusted redirect URI в настройках VK приложения
2. Убедиться что URL точно совпадает: `https://preview--infinite-business-card.poehali.dev/auth/vk`

## 📊 Проверка работоспособности

### 1. VK OAuth
```bash
# Проверка GET endpoint (получение auth_url)
curl "https://functions.poehali.dev/74d0ac96-7cc9-4254-86f4-508ca9a70f55?redirect_uri=https://preview--infinite-business-card.poehali.dev/auth/vk"

# Ожидается:
{
  "auth_url": "https://oauth.vk.com/authorize?client_id=...&redirect_uri=..."
}
```

### 2. Frontend логи
Открыть DevTools → Console:
```javascript
// При клике на "VK ID" должно быть:
VK Auth: redirect_uri = https://preview--infinite-business-card.poehali.dev/auth/vk
VK Auth response: { auth_url: "https://oauth.vk.com/..." }
Redirecting to VK: https://oauth.vk.com/...

// После редиректа с VK:
VK Callback: code = xxx redirect_uri = ...
VK Callback response status: 200
VK Callback data: { token: "...", user: {...}, card_slug: "..." }
```

### 3. Telegram Widget
```javascript
// При успешной авторизации:
Telegram auth callback: { id: 123, first_name: "...", photo_url: "..." }
```

## 🔄 Workflow авторизации

### VK OAuth:
1. Пользователь нажимает "VK ID"
2. Frontend вызывает GET `/vk-auth?redirect_uri=...`
3. Backend возвращает `auth_url` от VK
4. Редирект на VK OAuth
5. Пользователь разрешает доступ
6. VK редиректит на `/auth/vk?code=xxx`
7. Frontend отправляет POST `/vk-auth` с code
8. Backend:
   - Обменивает code на access_token
   - Получает данные пользователя из VK API
   - Создаёт/находит пользователя в БД
   - Генерирует JWT токен
   - Возвращает token + user + card_slug
9. Frontend сохраняет в localStorage и редиректит на визитку

### Telegram Widget:
1. Виджет загружается на страницу
2. Пользователь кликает "Log in with Telegram"
3. Открывается Telegram (web/app)
4. Пользователь подтверждает
5. Telegram вызывает `window.onTelegramAuthCallback(user)`
6. Frontend отправляет POST `/telegram-auth` с user data
7. Backend создаёт/находит пользователя и возвращает токен
8. Frontend сохраняет и перезагружает страницу

## 📝 Следующие шаги

1. **Настроить VK приложение** и добавить секреты
2. **Настроить Telegram бота** и добавить токен
3. **Протестировать авторизацию** на preview домене
4. **Добавить обработку ошибок** для edge cases
5. **Настроить мониторинг** авторизаций через Yandex Cloud Logging

## 🚀 Дополнительные улучшения (опционально)

- Добавить rate limiting на авторизацию (5 попыток в 5 минут)
- Добавить двухфакторную аутентификацию через email
- Логировать все попытки авторизации в БД
- Добавить OAuth через Яндекс (YANDEX_CLIENT_ID уже настроен)
- Кэшировать VK user info для уменьшения запросов к API

---

**Статус:** ✅ Код исправлен, требуется настройка секретов
**Дата:** 2026-01-04
**Коммит:** 5beb640
